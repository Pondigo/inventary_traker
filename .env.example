# ===========================================
# Inventory Tracker - Environment Variables
# ===========================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# PostgreSQL database URL for production
# Format: postgresql://username:password@hostname:port/database_name
DATABASE_URL=postgresql://username:password@localhost:5432/inventary_traker_prod

# Database connection pool size (default: 10)
POOL_SIZE=10

# Enable IPv6 for database connection (true/false)
ECTO_IPV6=false

# ===========================================
# PHOENIX CONFIGURATION  
# ===========================================
# Secret key base for signing cookies and sessions
# Generate with: mix phx.gen.secret
SECRET_KEY_BASE=your_secret_key_base_here_64_characters_minimum_length_required

# Phoenix server hostname (your domain)
PHX_HOST=pondi.app

# Phoenix server port (default: 4000)
PORT=4000

# Enable Phoenix server (set to "true" for production)
PHX_SERVER=true

# DNS cluster query (for distributed deployments, optional)
DNS_CLUSTER_QUERY=

# ===========================================
# CLOUDFLARE WORKERS EMAIL CONFIGURATION
# ===========================================
# Enable Cloudflare Workers for email sending (true/false)
# Set to false to use local mailbox at /dev/mailbox in development
USE_CLOUDFLARE_EMAIL=false

# Shared API key between Phoenix app and Cloudflare Worker
# Generate a secure random string: openssl rand -base64 32
CLOUDFLARE_WORKER_API_KEY=your_secure_random_api_key_here

# Cloudflare Worker URL (use your actual worker domain)
CLOUDFLARE_WORKER_URL=https://inventory-tracker-email.your-subdomain.workers.dev/send

# ===========================================
# DEVELOPMENT ONLY VARIABLES
# ===========================================
# These are only used in development mode

# Development database URL
DEV_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inventary_traker_dev

# Test database URL  
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inventary_traker_test

# ===========================================
# OPTIONAL DEPLOYMENT VARIABLES
# ===========================================
# These may be needed depending on your deployment platform

# Enable SSL for database connection (true/false)
DATABASE_SSL=true

# Force SSL connections (true/false)  
FORCE_SSL=true

# Enable HSTS (HTTP Strict Transport Security)
ENABLE_HSTS=true

# ===========================================
# MONITORING & OBSERVABILITY (Optional)
# ===========================================
# Add these if you want to integrate monitoring tools

# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn-here

# New Relic license key
# NEW_RELIC_LICENSE_KEY=your-license-key-here

# Application name for monitoring
# APP_NAME=inventory-tracker

# ===========================================
# ADDITIONAL SERVICES (Optional)
# ===========================================
# Add these if you integrate additional services

# Redis URL for caching/sessions
# REDIS_URL=redis://localhost:6379

# S3 or compatible storage for file uploads
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key  
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1

# ===========================================
# INSTRUCTIONS
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values
# 3. Never commit .env to version control
# 4. Add .env to your .gitignore file
# 5. Set these as environment variables in production
#
# For Cloudflare Workers email setup:
# - Deploy the worker: cd cloudflare-worker && npx wrangler deploy
# - Set worker secrets: npx wrangler secret put PHOENIX_API_KEY
# - Generate API key: openssl rand -base64 32
# - Set USE_CLOUDFLARE_EMAIL=true to enable real email sending
# - Set USE_CLOUDFLARE_EMAIL=false to use local mailbox at /dev/mailbox
#
# Generate secrets:
# - SECRET_KEY_BASE: mix phx.gen.secret
# - CLOUDFLARE_WORKER_API_KEY: openssl rand -base64 32